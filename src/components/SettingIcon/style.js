import {StyleSheet} from 'react-native';

const style = StyleSheet.create({
  setting: {
    width: 30,
    height: 30,
    marginTop: 'auto',
    marginBottom: 'auto',
  },
});

export default style;
