import {StyleSheet} from 'react-native';

const style = StyleSheet.create({
  setting: {
    marginHorizontal: 20,
    width: 24,
    height: 24,
    marginTop: 'auto',
    marginBottom: 'auto',
  },
});

export default style;
