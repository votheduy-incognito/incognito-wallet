import {StyleSheet} from 'react-native';

const style = StyleSheet.create({
  setting: {
    width: 35,
    height: 35,
    marginTop: 'auto',
    marginBottom: 'auto',
  },
});

export default style;
