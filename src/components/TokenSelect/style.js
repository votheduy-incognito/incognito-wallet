import { StyleSheet } from 'react-native';

const style = StyleSheet.create({
  container: {
    flexDirection: 'row',
  },
  select: {
    position: 'absolute',
    right: -40,
    bottom: -10,
  },
});

export default style;
