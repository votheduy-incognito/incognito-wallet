import { StyleSheet } from 'react-native';

const style = StyleSheet.create({
  container: {
    flexDirection: 'row',
  },
  select: {
    position: 'absolute',
    right: -40,
    bottom: -16,
    height: 54,
  },
  iconContainer: {
    width: 200,
    paddingLeft: 150,
  },
});

export default style;
