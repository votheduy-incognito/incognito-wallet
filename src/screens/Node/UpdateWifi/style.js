import { StyleSheet } from 'react-native';
import { COLORS } from '@src/styles';

export default StyleSheet.create({
  input: {
    paddingHorizontal: 0,
  },
  button: {
    marginTop: 50,
  },
  error: {
    color: COLORS.orange,
    fontSize: 14,
  },
});

