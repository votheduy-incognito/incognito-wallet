import React from 'react';
import {Text} from '@src/components/core';

class Pool extends React.Component {
  render() {
    return (
      <Text>
        Pool
      </Text>
    );
  }
}

export default Pool;
